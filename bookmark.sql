

-- create table bookmarks (
--   id serial4 primary key,
--   url varchar(255) not null,
--   name varchar(255) not null,
--   genre varchar(255) not null,
--   details text
  
-- );

-- insert into bookmarks (url, name, genre, details) values ('https://www.google.co.uk/', 'Google', 'Search Engine', 'Worlds most popular search engine');